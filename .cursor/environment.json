{
  "name": "ScoreSmarter",
  "install": "bash .cursor/install.sh",
  "terminals": [
    {
      "name": "web-dev-server",
      "command": "export NVM_DIR=\"$HOME/.nvm\"; . \"$NVM_DIR/nvm.sh\"; nvm use default >/dev/null; cd sailbrowser-web && npm run dev -- --host 0.0.0.0"
    }
  ],
  "ports": [3000]
}
