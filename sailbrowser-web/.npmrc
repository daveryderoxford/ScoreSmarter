# @storybook/angular@10.x peers cap at Angular 21 (<22). This project uses Angular 22.
# legacy-peer-deps is required for npm ci on App Hosting (and local install) until Storybook ships Angular 22 peers.
legacy-peer-deps=true
